Place the two brand images here with EXACTLY these names:

  app_icon.png   = Image 1 (the rounded-square green app icon)  -> used for the launcher icon
  splash.png     = Image 2 (the full "FootRank" lockup)         -> used for the loading splash

Recommended sizes:
  app_icon.png : 1024 x 1024 px
  splash.png   : ~1200 px wide (transparent or white background)
